const Favorites = {
  render: async () => {
    return `
      <main class="main">
        <div class="container">
            <h3>Favorite list</h3>
          </div>
        </div>
        </main>
    `;
  },

  after_render: async () => {},
};

export default Favorites;
